{
  "name": "meridian-railway",
  "private": true,
  "version": "0.1.0",
  "description": "Meridian Railway — a modular train simulator",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "play": "bash scripts/play.sh",
    "dev:test": "vite --host 127.0.0.1 --port 5174 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
